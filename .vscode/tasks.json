{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "python -m unittest discover -v -s tests -p *_test.py",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}