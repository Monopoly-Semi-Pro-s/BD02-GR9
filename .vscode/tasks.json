{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run all tests🧪",
            "type": "shell",
            "command": "python -m unittest discover -v -s tests -p *_test.py",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "Autopep8🚗",
            "type": "shell",
            "command": "python -m autopep8 --in-place --aggressive --aggressive --recursive .",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}